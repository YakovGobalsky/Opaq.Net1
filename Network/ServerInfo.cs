
using System.Net;

namespace Opaq.Network;

// public struct ServerInfo(string ip, int port) {
// 	public string ip { get; set; } = ip;
// 	public int port { get; set; } = port;

// 	public bool IsEmpty() => port == 0;

// 	public override string ToString() => $"{ip}:{port}";

// 	public System.Net.IPEndPoint GetEndPoint() => new System.Net.IPEndPoint(IPAddress.Parse(ip), port);
// }